{
  "buildCommand": "cd ns-faq && npm run build",
  "outputDirectory": "ns-faq/dist",
  "installCommand": "npm install && cd ns-faq && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
